services:
  rnudb:
    image: ghcr.io/computational-rare-disease-genomics-whg/rnudb:latest
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
